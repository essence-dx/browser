# AGENTS — Chromium Migration (3 Lanes, 100% Complete, Hours — Main Task Only)

**Branch:** `chromium-migration` from `dev@8df45e5` (Firefox 155.0.1)
**Goal:** 100% complete Firefox Gecko → Chromium. 3 agents, 3 parallel lanes, same branch, no tests, no merges. Lanes finish = browser boots on Chromium. Current status (verified 2026-09-24, HEAD `01eb20c` — AGENTS task list below unchanged): app-only grep is 0 BUT the tree does not run — see TODO.md (R1–R4 repairs + G1–G4) and `docs/chromium-migration-audit.md` §7. `engine-chromium/` is a 31-file scaffold (not booted); 125 patches mapped-not-rewritten; ~20 IDL/CPP files kept with mojo shims alongside; `surfer.json` flag reads `"chromium"` but is unwired.
**Scope:** `src/zen` app 176 files / ~50k lines + 258 patches / 13k lines + 15 XPCOM/C++ files + `engine-chromium/` shell. 808 test files ignored.
**Done = 100%:** zero `Services\.|gBrowser\.|SessionStore\.|PlacesUtils\.|MozXULElement|createXULElement|ChromeUtils\.|XPCOMUtils\.` outside `src/zen/adapters/`, zero `chrome://|resource://|-moz-|@namespace|%include` in migrated files, `engine-chromium/` boots one window (tab 1 = Chromium, tab 2 = Gecko fallback), flag flips to `"chromium"`. Not "imports added" — every call site actually swapped and working.

## Ground Rules (Hours, 100%)

- **No tests, no merges, no heavy commands.** Direct commits to `chromium-migration` in owned dirs only. No `npm`/`python`/`mach`/`surfer`/`git merge`. `dev` untouched.
- **100% means every call site swapped, not commented.** Each `Services.X` → `adapters/` import + working call. Each `gBrowser.Y` → `adapters/tabs` working call. Each `MozXULElement`/`createXULElement` → `adapters/xul` working call. Each `chrome://` import → relative `../` import. Each `resource://` → adapter or `shared/`. Each `-moz-`/`@namespace`/`%include` in CSS → standard CSS. Stub comments alone do not count.
- **Zero overlap:** one directory = one lane. Never edit outside your lane. Need another lane's file → ask Lane 1 for adapter.
- **Commits:** `chore(migration): lane{N}: <files> <done/total>` + `Co-authored-by: CommandCodeBot <noreply@commandcode.ai>`. Report `grep` count before/after per commit.

## Lane Ownership — 100% Task List

| Lane | Agent | Owns (only this agent) | 100% task — migrate EVERY call site, then shell + cutover |
|------|-------|------------------------|------------------------------------------------------------|
| **1 — Foundation + Cutover** | Agent 1 | `surfer.json`, `src/zen/shared/**`, `src/zen/adapters/**`, `src/zen/zen.globals.mjs`, `docs/chromium-migration.md`, `engine-chromium/**` (new), `src/zen/moz.build` | 1a. Keep `shared/zenColorUtils` + `zenSplitLayout` pure (done). 1b. `adapters/{prefs,tabs,session,xul,observers,windows,storage}.mjs` — every export must have WORKING Chromium body behind `engine === "chromium"` check (not commented stub): `chrome.storage`, `chrome.tabs`, `chrome.sessions`, `chrome.windows`, `chrome.events`, DOM `createElement`/`template`. 1c. `zen.globals.mjs` — remove `Services/gBrowser/MozXULElement/SessionStore` from allowlist when done; keep `zenAdapters/*`. 1d. Create `engine-chromium/` shell: CEF/WebView2 fetch + `BrowserView` tab strip reusing Zen vertical-tabs design + dual-boot (tab 1 Chromium, tab 2 Gecko). 1e. Flip `surfer.json:migration.engine` → `"chromium"` only when grep counts hit zero and shell boots. 1f. Verify: `grep -R "Services\.\|gBrowser\." src/zen --exclude-dir=adapters` = 0. |
| **2 — UI Shell 100%** | Agent 2 | `src/zen/common/**` (112 hits/17 files), `src/zen/tabs/**` (42/2), `src/zen/spaces/**` (142/7), `src/zen/compact-mode/**` incl `ZenMouseTracker.cpp` + `.idl`, `src/zen/split-view/**` (69/1), `src/zen/welcome/**`, `src/zen/media/**`, `src/zen/kbs/**`, `src/zen/folders/**` (ZenFolders 54), `src/zen/glance/**`, all owned `*.css`/`*.inc.xhtml`/`jar.inc.mn` | 2a. EVERY `Services.prefs/obs` → `adapters/prefs`/`observers` import + call. 2b. EVERY `gBrowser.*` (`selectedTab`, `tabContainer`, `pinTab`, `linkedBrowser`, `docShellIsActive`) → `adapters/tabs` + `session`. 2c. EVERY `MozXULElement.parseXULToFragment`/`createXULElement` → `adapters/xul`. 2d. EVERY `chrome://` import → relative path; EVERY `resource://` → adapter/`shared/`. 2e. EVERY `-moz-`/`@namespace`/`%include` in owned CSS → standard CSS + CSS vars (`vertical-tabs.css` 19 `-moz-` hits, `zen-workspaces.css` 14, `zen-theme.css` 22 must go). 2f. `compact-mode/ZenMouseTracker.cpp` + `nsIZenMouseTracker.idl` → `IntersectionObserver` + `chrome.windows` shim (keep `.cpp` compiling or delete behind flag). 2g. Per-file checklist in TODO.md — check off each file only when its grep count = 0. |
| **3 — Services, Data, Patches 100%** | Agent 3 | `src/zen/boosts/**`, `src/zen/live-folders/**`, `src/zen/sync/**` (Applier 27 hits), `src/zen/urlbar/**` (SiteDataPanel 53, UBGlobalActions 36, UB provs), `src/zen/sessionstore/**` (WindowSync 42), `src/zen/space-routing/**` (Dialog 39), `src/zen/mods/**`, `src/zen/toolkit/**`, `src/zen/drag-and-drop/**` (38), `src/zen/window-drag/**`, `src/zen/share/**`, `src/zen/downloads/**`, `src/browser/**` (258 patches), `src/external-patches/**`, `prefs/**`, `configs/**`, XPCOM `.idl`/`.cpp`/`.h`/`components.conf` | 3a. EVERY `Services.*`/`ChromeUtils.*`/`XPCOMUtils.*` in owned `.mjs` → adapters. 3b. `sync/` Weave → `chrome.storage.sync` + `identity` working calls. 3c. `urlbar/` providers → `chrome.omnibox` working calls. 3d. `sessionstore/` → `chrome.sessions` working calls. 3e. `mods`/`boosts` `IOUtils`/`PathUtils` → `adapters/storage` working calls. 3f. EVERY `nsIZen*.idl` + `nsZen*.cpp`/`Zen*Cache.cpp` + `components.conf` → Mojo/`chrome.scripting` working shim or delete behind `engine` flag (files: `compact-mode/nsIZenMouseTracker.idl`, `window-drag/nsIZenWindowDragUtils.idl`+`.cpp`, `mods/nsIZenModsBackend.idl`, `drag-and-drop/nsIZenDragAndDrop.idl`, `boosts/nsZenBoostsBackend.cpp`, `toolkit/ZenShareInternal.cpp`). 3g. ALL 258 `src/browser/**/*.patch` + `toolkit/dom/layout` patches → rewrite to `BUILD.gn` + Views equivalents file-by-file (not log-only). 3h. `prefs/*.yaml` + `configs/` → Chromium `PrefService`/policy. |

**Rule:** cross-cutting → Lane 1 adapter PR first, then lanes 2–3 consume. `TODO.md` per-file checklist is the source of truth — check only when file grep = 0.

## How to Start Now (Each Agent — 100%)

- **Lane 1:** `git checkout chromium-migration` → make every `adapters/*.mjs` export dual-engine working (Gecko now + `chrome.*` live behind flag) → scaffold `engine-chromium/` → commit `lane1: X/Y adapters live`.
- **Lane 2:** same checkout → per TODO.md file list, swap every call site in owned files → `grep` file = 0 → commit `lane2: <file> 0 hits`.
- **Lane 3:** same checkout → per TODO.md file list, swap every call site + IDL/CPP + patches → commit `lane3: <file> 0 hits`.

Done = all three lanes report 0 remaining hits in owned files + shell boots + flag flips. That is 100%.
