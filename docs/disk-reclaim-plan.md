# Disk reclaim plan — what is safe to delete on G:

**Status:** scan only. **Nothing has been deleted.**
**Method:** `python3 .workbuddy-ai/scan_rebuildable.py` — read-only, prunes a directory once it
is identified and sized. Full output: `.workbuddy-ai/scan-report.txt` (2,646 lines).

```
dirs visited: 945,148    unreadable: 8    candidates: 2,639    reported: 70.61 GB
```

## Why this matters

The Chromium lane needs **78 GB free on one volume** (28 checkout + 40 `out/` + 10 toolchain).
G: currently has **29 GB** — 49 GB short. This scan is about closing that gap with things that
can be regenerated rather than lost.

## Totals by category

| Category | Size | Regenerated by |
|---|---:|---|
| `node_modules` | 47.82 GB | `npm`/`pnpm`/`yarn`/`bun install` |
| Rust `target/` | 13.89 GB | `cargo build` (slow — see caveat) |
| `.next` / `.turbo` / build caches | 3.81 GB | `next build` |
| Python venvs | 3.51 GB | `pip install -r requirements.txt` |
| pkg caches (`.gradle`, `.cargo`, `.nuget`, `.npm`) | 1.68 GB | re-downloaded on next fetch |
| `__pycache__` | 0.09 GB | automatic |
| **total** | **70.61 GB** | |

### Caveat: junctions inflate the number

Scoop lays tools out as junctions, so one set of files appears under several paths:

```
4.79 GB  G:\Dev\Tools\Scoop\persist\bun\install\global\node_modules
4.79 GB  G:\Dev\Tools\Scoop\apps\bun\current\install\global\node_modules     <- junction
4.79 GB  G:\Dev\Tools\Scoop\apps\bun\1.3.13\install\global\node_modules      <- junction
1.84 GB  G:\Dev\Tools\Scoop\apps\nodejs\current\node_modules                 <- junction
1.84 GB  G:\Dev\Tools\Scoop\apps\nodejs\26.4.0\node_modules
```

Confirmed overcount: **6.63 GB**. The `persist\bun` ↔ `apps\bun\current\install\global`
relationship likely adds a further ~4.79 GB, so the honest figure is:

**Realistic reclaimable: 59–64 GB** (not 70.61 GB).

Against a 49 GB shortfall that clears it, leaving roughly **10–15 GB of headroom** on G:. That
is workable but tight — Chromium's `out/` grows as you iterate.

## Biggest individual entries

| Size | Path |
|---:|---|
| 7.68 GB | `G:\Dx\bot\src-tauri\target` |
| 7.20 GB | `G:\Essence\inspirations\plate\node_modules` |
| 4.79 GB | `G:\Dev\Tools\Scoop\...\bun\...\global\node_modules` (×3 junction, ~4.79 real) |
| 3.24 GB | `G:\Dx\desktop\web\Graph\node_modules` |
| 2.78 GB | `G:\Dx\extensions\node_modules` |
| 2.64 GB | `G:\osxcross\target` |
| 2.43 GB | `G:\Dx\bot\node_modules` |
| 1.84 GB | `G:\Dev\Tools\Scoop\...\nodejs\...\node_modules` (×2 junction) |
| 1.71 GB | `G:\Dx\.wb-acp-verify\target` |
| 0.99 GB | `G:\Dx\desktop\web\Video\.next` |
| 0.94 GB | `G:\Ai\.venv` |
| 0.93 GB | `G:\HEXXED-8_20_2026\needle\.venv` |
| 0.70 GB | `G:\Workspaces\ai\.gradle` |

## Caveats before deleting anything

**`G:\Dx\` is your own product** (per `USER.md`). 25.77 GB of the candidates live there.
`G:\Dx\bot\src-tauri\target` alone is 7.68 GB — the single biggest item, and a full Rust/Tauri
rebuild is tens of minutes. Rebuildable, but it costs real time.

**Do not delete:**

| Path | Why |
|---|---|
| `G:\Archives\WorkBuddyAI\resources\app.asar.unpacked\{,cli}\node_modules` (0.28 GB) | the running WorkBuddy install — this would break the app doing the deleting |
| `G:\Zen\node_modules` (0.11 GB) | required by `surfer` / the Zen build scripts |
| `G:\Zen\tools\ffprefs\target` (0.08 GB) | Rust helper used by the build |
| `G:\Dev\Tools\Scoop\apps\*\current\*` | junctions; deleting through them breaks the tool, and frees the space only once |

**Already condemned, separate category:** 3.61 GB sits under paths literally named `Trash`
(`G:\Trash\...`, `G:\Archives\E-Trash\Trash_moved_20260222_035727\...`). These are not
rebuildable — they are deletions you already decided on. Worth clearing, but not part of the
70.61 GB above.

## Recommendation

Clearing `node_modules` + `target/` + `.next` + venvs across `G:\Dx`, `G:\Essence`, `G:\HEXXED-*`,
`G:\Trash` and `G:\ZZZ_*` is enough to clear the 49 GB shortfall with headroom, and every one of
those is regenerable. Start with the 7.68 GB `src-tauri\target` and the 7.20 GB
`plate\node_modules` — 14.88 GB from two paths, both rebuildable.

Say which of these you want removed and I will verify each path's size immediately before
deleting it, one batch at a time.
